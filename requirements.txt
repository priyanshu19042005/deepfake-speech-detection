# CPU-only PyTorch wheel — small build, no CUDA download.
--extra-index-url https://download.pytorch.org/whl/cpu

torch
numpy>=1.26
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3
streamlit>=1.30
matplotlib
# Force a recent pyarrow that ships prebuilt wheels (older versions try to
# compile from source on the deploy host and fail).
pyarrow>=17

# CPU-only PyTorch wheel — keeps the Streamlit Cloud build small and avoids the
# large CUDA download. Pins are loosened so pip can resolve wheels that match
# whatever Python version the deployment host uses (Linux), rather than the exact
# Windows/Python-3.13 versions used during development.
--extra-index-url https://download.pytorch.org/whl/cpu

torch
librosa>=0.10
soundfile>=0.12
numpy<2.3
scikit-learn>=1.3
streamlit>=1.30
matplotlib
